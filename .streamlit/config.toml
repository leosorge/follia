# .streamlit/secrets.toml
# Rinomina questo file in secrets.toml e NON committarlo su Git.
# Usalo solo per sviluppo locale oppure configura i Secret su Streamlit Cloud.

DEEPGRAM_API_KEY = "la-tua-chiave"
OPENAI_API_KEY   = "sk-..."
WP_URL           = "https://www.tuosito.it"
WP_USERNAME      = "utente"
WP_PASSWORD      = "app-password"
